server {
    listen 80;
    server_name localhost;
    
    # 静态文件
    location /static/ {
        alias /home/ubuntu/timesheet/static/;
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
    
    # HTML文件
    location / {
        root /home/ubuntu/timesheet;
        try_files $uri $uri/ @fastapi;
    }

    # Statistics 特殊路由
    location = /statistics.html {
        alias /home/ubuntu/timesheet/statistics/statistics.html;
        try_files /home/ubuntu/timesheet/statistics/statistics.html =404;
    }

    # Statistics文件夹
    location /statistics/ {
        alias /home/ubuntu/timesheet/statistics/;
        try_files $uri =404;
    }
    
    # FastAPI后端
    location @fastapi {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
    
    # API路由 - 代理到schedule-api服务
    location /api/ {
        proxy_pass http://127.0.0.1:5004;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
    
    # 健康检查 - 代理到schedule-api服务
    location /health {
        proxy_pass http://127.0.0.1:5004;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
} 